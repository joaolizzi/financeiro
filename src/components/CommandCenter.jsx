import React,{useEffect,useMemo,useRef,useState} from 'react';
import {Search,LayoutDashboard,ReceiptText,CreditCard,Target,Sparkles,Plus,Pencil,Command,ArrowRight,Zap,Upload,Clock3,FileSpreadsheet,LoaderCircle,CheckCircle2,Repeat,CalendarClock,AlertTriangle,ArrowDownCircle,ArrowUpCircle} from 'lucide-react';
import * as XLSX from 'xlsx';
import {supabase} from '../lib/supabase';
import './CommandCenter.css';

const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const normalize=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
const categories=['Moradia','Alimentação','Transporte','Lazer','Compras','Contas','Assinaturas','Investimentos','Outros'];
const aliases={date:['data','date','data da compra','data compra'],description:['descricao','description','historico','lançamento','lancamento','estabelecimento'],value:['valor','value','amount','total','valor total'],category:['categoria','category'],type:['tipo','type','natureza','movimento','operacao','operação']};
const getValue=(row,names)=>{const map=Object.fromEntries(Object.entries(row).map(([k,v])=>[normalize(k),v]));for(const n of names){if(map[normalize(n)]!==undefined)return map[normalize(n)]}return ''};
const parseSignedMoney=v=>{if(typeof v==='number')return Number(v)||0;let s=String(v||'').replace(/[^0-9,().-]/g,'').trim();if(!s)return 0;const neg=/^-|\(.+\)/.test(s);s=s.replace(/[()]/g,'');if(s.includes(',')&&s.includes('.'))s=s.replace(/\./g,'').replace(',','.');else if(s.includes(','))s=s.replace(',','.');const n=Math.abs(Number(s)||0);return neg?-n:n};
const parseDate=v=>{if(v instanceof Date&&!Number.isNaN(v.getTime()))return v.toISOString().slice(0,10);const s=String(v||'').trim();if(/^\d{4}-\d{2}-\d{2}$/.test(s))return s;const br=s.match(/^(\d{1,2})[\/.-](\d{1,2})[\/.-](\d{2,4})$/);if(br){const y=br[3].length===2?`20${br[3]}`:br[3];return `${y}-${br[2].padStart(2,'0')}-${br[1].padStart(2,'0')}`}const d=new Date(s);return Number.isNaN(d.getTime())?'':d.toISOString().slice(0,10)};
const inferCategory=text=>{const s=normalize(text);if(/mercado|ifood|restaurante|padaria|comida|lanche/.test(s))return'Alimentação';if(/uber|99|gasolina|combustivel|posto|onibus/.test(s))return'Transporte';if(/netflix|spotify|prime|assinatura/.test(s))return'Assinaturas';if(/aluguel|condominio|casa/.test(s))return'Moradia';if(/luz|energia|agua|internet|telefone/.test(s))return'Contas';if(/cinema|jogo|bar|show/.test(s))return'Lazer';if(/amazon|shopee|mercado livre|loja/.test(s))return'Compras';return'Outros'};
const inferKind=(rawType,signed,description)=>{const t=normalize(rawType),d=normalize(description);if(/entrada|receita|credito|crédito|salario|salário|renda|deposito|depósito/.test(t)||/salario|salário|pix recebido|transferencia recebida|transferência recebida/.test(d))return'entrada';if(/saida|saída|despesa|debito|débito|gasto|compra/.test(t))return'gasto';return signed<0?'gasto':'gasto'};
const fingerprint=r=>`${r.data}|${normalize(r.descricao)}|${Number(r.valor).toFixed(2)}`;
const months={janeiro:1,fevereiro:2,marco:3,abril:4,maio:5,junho:6,julho:7,agosto:8,setembro:9,outubro:10,novembro:11,dezembro:12};
const parseVisibleMonth=()=>{const text=normalize(document.querySelector('.history-nav h2')?.textContent||'');const name=Object.keys(months).find(m=>text.includes(m)),year=Number(text.match(/\b(20\d{2})\b/)?.[1]);return name&&year?{month:months[name],year}:null};

export default function CommandCenter({open,onClose,onNavigate,onNewExpense,onQuickAdd,onIncome}){
 const[q,setQ]=useState(''),[results,setResults]=useState([]),[searching,setSearching]=useState(false),[importRows,setImportRows]=useState([]),[importing,setImporting]=useState(false),[importName,setImportName]=useState(''),[importError,setImportError]=useState('');
 const fileRef=useRef(null),jumpTimer=useRef(null);
 useEffect(()=>{if(open){setQ('');setResults([]);setImportRows([]);setImportName('');setImportError('')}},[open]);
 useEffect(()=>()=>clearTimeout(jumpTimer.current),[]);
 const actions=useMemo(()=>[
  {label:'Visão geral',hint:'Dashboard principal',icon:LayoutDashboard,run:()=>onNavigate('overview')},{label:'Timeline',hint:'Todos os eventos financeiros',icon:Clock3,run:()=>onNavigate('timeline')},{label:'Lançamentos',hint:'Histórico e filtros',icon:ReceiptText,run:()=>onNavigate('transactions')},{label:'Cartões',hint:'Faturas e limites',icon:CreditCard,run:()=>onNavigate('cards')},{label:'Planejamento',hint:'Metas e recorrências',icon:Target,run:()=>onNavigate('planning')},{label:'Análises',hint:'Insights e inteligência',icon:Sparkles,run:()=>onNavigate('insights')},{label:'Quick Add',hint:'Digite um gasto em linguagem natural',icon:Zap,run:onQuickAdd,accent:true},{label:'Novo gasto',hint:'Abrir formulário completo',icon:Plus,run:onNewExpense},{label:'Editar renda',hint:'Atualizar renda mensal',icon:Pencil,run:onIncome},{label:'Importar CSV / Excel',hint:'Prévia, entradas, gastos e proteção contra duplicados',icon:Upload,run:()=>fileRef.current?.click(),accent:true,importAction:true}
 ],[onNavigate,onNewExpense,onQuickAdd,onIncome]);
 const filtered=actions.filter(a=>`${a.label} ${a.hint}`.toLowerCase().includes(q.toLowerCase()));
 function finishTransactionJump(label){jumpTimer.current=setTimeout(()=>{const input=document.querySelector('.history-tools input');if(input){const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set;setter?.call(input,label);input.dispatchEvent(new Event('input',{bubbles:true}));input.focus()}jumpTimer.current=setTimeout(()=>{const rows=[...document.querySelectorAll('.sheet tbody tr')],row=rows.find(r=>normalize(r.textContent).includes(normalize(label)));if(row){row.scrollIntoView({behavior:'smooth',block:'center'});row.animate([{outline:'1px solid rgba(143,156,255,.85)',background:'rgba(143,156,255,.16)'},{outline:'1px solid transparent',background:'transparent'}],{duration:2200,easing:'ease-out'})}},180)},80)}
 function jumpToDate(targetView,date,label){if(!date){onNavigate(targetView);onClose();return}const[y,m]=String(date).slice(0,7).split('-').map(Number);if(!y||!m){onNavigate(targetView);onClose();return}onClose();onNavigate('transactions');let tries=0;const step=()=>{tries++;const current=parseVisibleMonth();if(!current){if(tries<45)jumpTimer.current=setTimeout(step,60);return}const cur=current.year*12+current.month,target=y*12+m;if(cur===target){if(targetView==='transactions')finishTransactionJump(label);else onNavigate(targetView);return}const buttons=document.querySelectorAll('.history-nav button');const button=target<cur?buttons[0]:buttons[buttons.length-1];if(!button||tries>48)return;button.click();jumpTimer.current=setTimeout(step,80)};jumpTimer.current=setTimeout(step,70)}
 useEffect(()=>{if(!open||q.trim().length<2){setResults([]);setSearching(false);return}let alive=true;const timer=setTimeout(async()=>{setSearching(true);const term=q.trim().replace(/[%_]/g,'');const{data:{user}}=await supabase.auth.getUser();if(!user){if(alive)setSearching(false);return}const[gastos,compras,cartoes,assinaturas,recorrentes]=await Promise.all([supabase.from('gastos').select('id,descricao,categoria,valor,data').eq('user_id',user.id).or(`descricao.ilike.%${term}%,categoria.ilike.%${term}%`).order('data',{ascending:false}).limit(8),supabase.from('credit_card_purchases').select('id,descricao,categoria,valor_total,data_compra,credit_cards(nome)').eq('user_id',user.id).or(`descricao.ilike.%${term}%,categoria.ilike.%${term}%`).order('data_compra',{ascending:false}).limit(8),supabase.from('credit_cards').select('id,nome,apelido,limite').eq('user_id',user.id).or(`nome.ilike.%${term}%,apelido.ilike.%${term}%`).limit(5),supabase.from('finance_subscriptions').select('id,nome,valor,dia,categoria,ativo').eq('user_id',user.id).ilike('nome',`%${term}%`).limit(5),supabase.from('finance_recurring').select('id,descricao,valor,dia,categoria,ativo').eq('user_id',user.id).ilike('descricao',`%${term}%`).limit(5)]);const items=[...(gastos.data||[]).map(x=>({id:`g-${x.id}`,label:x.descricao,hint:`${x.categoria} • ${money(x.valor)} • ${new Date(`${x.data}T12:00:00`).toLocaleDateString('pt-BR')}`,type:'Lançamento',icon:ReceiptText,run:()=>jumpToDate('transactions',x.data,x.descricao)})),...(compras.data||[]).map(x=>({id:`p-${x.id}`,label:x.descricao,hint:`${x.credit_cards?.nome||'Cartão'} • ${money(x.valor_total)} • ${new Date(`${x.data_compra}T12:00:00`).toLocaleDateString('pt-BR')}`,type:'Compra no cartão',icon:CreditCard,run:()=>jumpToDate('cards',x.data_compra,x.descricao)})),...(cartoes.data||[]).map(x=>({id:`c-${x.id}`,label:x.apelido||x.nome,hint:`${x.nome} • Limite ${money(x.limite)}`,type:'Cartão',icon:CreditCard,run:()=>{onNavigate('cards');onClose()}})),...(assinaturas.data||[]).map(x=>({id:`s-${x.id}`,label:x.nome,hint:`${x.categoria||'Assinaturas'} • ${money(x.valor)} • dia ${x.dia}`,type:'Assinatura',icon:CalendarClock,run:()=>{onNavigate('planning');onClose()}})),...(recorrentes.data||[]).map(x=>({id:`r-${x.id}`,label:x.descricao,hint:`${x.categoria||'Recorrente'} • ${money(x.valor)} • dia ${x.dia}`,type:'Recorrente',icon:Repeat,run:()=>{onNavigate('planning');onClose()}}))];if(alive){setResults(items);setSearching(false)}},240);return()=>{alive=false;clearTimeout(timer)}},[q,open,onNavigate]);
 if(!open)return null;
 function execute(a){a.run();if(!a.importAction&&!a.id)onClose()}
 async function readFile(e){const file=e.target.files?.[0];e.target.value='';if(!file)return;setImportName(file.name);setImportError('');try{const buffer=await file.arrayBuffer();const wb=XLSX.read(buffer,{type:'array',cellDates:true});const ws=wb.Sheets[wb.SheetNames[0]];const raw=XLSX.utils.sheet_to_json(ws,{defval:'',raw:false});let rows=raw.map((row,i)=>{const descricao=String(getValue(row,aliases.description)||'').trim(),signed=parseSignedMoney(getValue(row,aliases.value)),valor=Math.abs(signed),data=parseDate(getValue(row,aliases.date)),rawCat=String(getValue(row,aliases.category)||'').trim(),rawType=getValue(row,aliases.type),kind=inferKind(rawType,signed,descricao),cat=categories.includes(rawCat)?rawCat:inferCategory(descricao);return{id:i+1,descricao,valor,data,categoria:cat,tipo:normalize(rawType).includes('fix')?'fixo':'variavel',kind,selected:true,duplicate:false}}).filter(r=>r.descricao&&r.valor>0&&r.data);if(!rows.length){setImportRows([]);setImportError('Nenhuma linha válida encontrada. Verifique as colunas Data, Descrição e Valor.');return}const{data:{user}}=await supabase.auth.getUser();if(user){const dates=rows.filter(r=>r.kind==='gasto').map(r=>r.data).sort();if(dates.length){const{data:existing}=await supabase.from('gastos').select('descricao,valor,data').eq('user_id',user.id).gte('data',dates[0]).lte('data',dates[dates.length-1]);const set=new Set((existing||[]).map(fingerprint));rows=rows.map(r=>r.kind==='gasto'&&set.has(fingerprint(r))?{...r,duplicate:true,selected:false}:r)}}setImportRows(rows)}catch(err){setImportRows([]);setImportError(`Não foi possível ler o arquivo${err?.message?`: ${err.message}`:'.'}`)}}
 function patchRow(id,patch){setImportRows(rows=>rows.map(r=>r.id===id?{...r,...patch}:r))}
 const selectedRows=importRows.filter(r=>r.selected&&!r.duplicate),selectedExpenses=selectedRows.filter(r=>r.kind==='gasto'),selectedIncome=selectedRows.filter(r=>r.kind==='entrada'),duplicateCount=importRows.filter(r=>r.duplicate).length;
 async function importAll(){if(!selectedRows.length)return;setImporting(true);setImportError('');const{data:{user}}=await supabase.auth.getUser();if(!user){setImporting(false);setImportError('Sessão expirada. Entre novamente.');return}try{if(selectedExpenses.length){const payload=selectedExpenses.map(({descricao,valor,data,categoria,tipo})=>({user_id:user.id,descricao,valor,data,categoria,tipo}));const{error}=await supabase.from('gastos').insert(payload);if(error)throw error}if(selectedIncome.length){const grouped={};selectedIncome.forEach(r=>{const[y,m]=r.data.slice(0,7).split('-').map(Number),k=`${y}-${m}`;grouped[k]=(grouped[k]||0)+Number(r.valor)});for(const[k,value]of Object.entries(grouped)){const[y,m]=k.split('-').map(Number);const{data:current,error:readError}=await supabase.from('rendas').select('valor').eq('user_id',user.id).eq('mes',m).eq('ano',y).maybeSingle();if(readError)throw readError;const{error}=await supabase.from('rendas').upsert({user_id:user.id,mes:m,ano:y,valor:Number(current?.valor||0)+value},{onConflict:'user_id,mes,ano'});if(error)throw error}}setImporting(false);onNavigate('transactions');onClose();setTimeout(()=>window.location.reload(),120)}catch(err){setImporting(false);setImportError(`Erro ao importar: ${err.message||'falha desconhecida'}`)}}
 return <div className="command-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="command-center" role="dialog" aria-modal="true"><input ref={fileRef} hidden aria-hidden="true" tabIndex="-1" type="file" accept=".csv,.xlsx,.xls" onChange={readFile}/><div className="command-search"><Search size={19}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Busque gastos, compras, cartões, assinaturas ou recorrentes..."/><kbd>ESC</kbd></div>{importRows.length||importError?<div className="command-import"><div className="command-import-head"><FileSpreadsheet size={19}/><div><b>{importName||'Importação 2.0'}</b><span>{importRows.length?`${selectedRows.length} selecionados • ${duplicateCount} duplicados bloqueados`:'Revise o arquivo antes de tentar novamente'}</span></div><button onClick={()=>{setImportRows([]);setImportError('')}}>Cancelar</button></div>{importError&&<div className="command-import-error"><AlertTriangle size={15}/>{importError}</div>}{importRows.length>0&&<><div className="command-import-summary"><span><ArrowDownCircle size={14}/> {selectedExpenses.length} gastos</span><span><ArrowUpCircle size={14}/> {selectedIncome.length} entradas</span><span><AlertTriangle size={14}/> {duplicateCount} duplicados</span></div><div className="command-import-preview import-v2">{importRows.slice(0,30).map(r=><div key={r.id} className={`${r.duplicate?'duplicate':''} ${!r.selected?'disabled':''}`}><input type="checkbox" checked={r.selected&&!r.duplicate} disabled={r.duplicate} onChange={e=>patchRow(r.id,{selected:e.target.checked})}/><span>{new Date(`${r.data}T12:00:00`).toLocaleDateString('pt-BR')}</span><b>{r.descricao}</b><button className={`kind ${r.kind}`} onClick={()=>patchRow(r.id,{kind:r.kind==='gasto'?'entrada':'gasto',duplicate:false,selected:true})}>{r.kind==='gasto'?'Gasto':'Entrada'}</button><strong>{money(r.valor)}</strong>{r.duplicate&&<em>Duplicado</em>}</div>)}</div><p className="command-import-note">Entradas são somadas à renda do mês correspondente. Duplicados de gastos são desmarcados automaticamente.</p><button className="command-import-confirm" onClick={importAll} disabled={importing||!selectedRows.length}>{importing?<><LoaderCircle className="spin" size={15}/> Importando...</>:<><CheckCircle2 size={15}/> Importar {selectedRows.length} itens</>}</button></>}</div>:<><div className="command-caption"><span>{q.trim().length>=2?'BUSCA GLOBAL 2.0':'AÇÕES RÁPIDAS'}</span><span><Command size={12}/> K</span></div><div className="command-list">{q.trim().length>=2&&results.map(a=><button key={a.id} onClick={()=>execute(a)}><span className="command-icon"><a.icon size={18}/></span><span><b>{a.label}</b><small>{a.type} • {a.hint}</small></span><ArrowRight size={15}/></button>)}{q.trim().length>=2&&searching&&<div className="command-searching"><LoaderCircle className="spin" size={15}/> Buscando em todo o seu financeiro...</div>}{filtered.map((a,i)=><button key={a.label} className={`${a.accent?'accent':''} ${a.importAction?'import-trigger':''}`} onClick={()=>execute(a)}><span className="command-icon"><a.icon size={18}/></span><span><b>{a.label}</b><small>{a.hint}</small></span><ArrowRight size={15}/><em>{i+1}</em></button>)}{!filtered.length&&!results.length&&!searching&&<div className="command-empty">Nada encontrado no seu histórico.</div>}</div><footer><span>Busca em lançamentos, compras, cartões, assinaturas e recorrentes</span><span>Finanças Command Center 2.0</span></footer></>}</section></div>
}
